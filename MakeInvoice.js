/**
 * A transaction processor function that allows one to make an Invoice
 * @param {org.perishable.MakeInvoice} tx
 * @transaction
 */

async function makeInvoice(tx) {
  const factory = getFactory();
  const NS = "org.perishable";
}
